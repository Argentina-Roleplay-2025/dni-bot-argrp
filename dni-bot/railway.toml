[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "node scripts/importar-calificaciones.mjs && node dist/server/index.js"
restartPolicyType = "always"
